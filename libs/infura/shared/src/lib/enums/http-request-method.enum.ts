export enum InfuraMethod {
  TraceCall = 'trace_call',
  TraceFilter = 'trace_filter',
  EthNewFilter = 'eth_newFilter',
  EthGetFilterChanges = 'eth_getFilterChanges',
  EthSubscribe = 'eth_subscribe',
}
